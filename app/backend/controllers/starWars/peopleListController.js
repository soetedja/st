starWarsModule.controller('peopleListController', peopleListController);

function peopleListController($scope, $routeParams, $location, baseCommon, starWarsService) {
    //list handled in directive, look at loadMore directive in commonDirective.js
}